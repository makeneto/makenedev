export interface Blog {
  image: string
  title: string
  description: string
  date: string
  slug: string
  publishedAt: string
  createdAt: string
}

export interface Heading {
  id: string
  text: string
}
